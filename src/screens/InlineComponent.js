import styled, {css} from 'styled-components'
import React, { useState } from "react";

export default function InlineComponent() {

  return (
    <div>
      <h1 style={{
        color: "red",
        fontSize:40
      }}>Exemplo de css inline</h1>
    </div>
  );
}